import 'package:flutter/material.dart';

class ErrorqrPage extends StatelessWidget {


  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('prueba'),
      ),
    );
  }
}